export interface Event {
       id: string;
       name: string;
       date: Date;
       capacity: number;
       registrationCount: number;
       category: string;
       createdAt: Date;
       updatedAt: Date;
}